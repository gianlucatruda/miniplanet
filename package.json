{
  "name": "micro-planet-app",
  "version": "1.0.0",
  "description": "A WebSocket server and Three.js client application",
  "main": "server/index.js",
  "scripts": {
    "start:server": "ts-node server/index.ts",
    "build": "tsc",
    "serve": "http-server . -c-1",
    "start": "npm-run-all --parallel start:server serve"
  },
  "dependencies": {
    "ws": "^8.0.0",
    "three": "^0.140.0"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "typescript": "^4.0.0",
    "http-server": "^14.1.1",
    "npm-run-all": "^4.1.5"
  }
}
